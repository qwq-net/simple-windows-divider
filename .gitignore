# Cargo のビルド成果物
/target

# rustfmt のバックアップファイル
**/*.rs.bk

# MSVC ビルドが生成するデバッグ情報
*.pdb

# 実行時ログ（既定の出力先は %APPDATA% だが、リポジトリ内に出た場合に備える）
*.log

# Cargo.lock はアプリ（バイナリ）のためコミットする。ここでは無視しない。

# Claude
docs/superpowers/